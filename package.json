{
  "name": "enhanced-table",
  "version": "1.4.3",
  "description": "This visualization plugin is like a Data Table, but with enhanced features like computed columns and filter bar",
  "main": "index.js",
  "kibana": {
    "version": "kibana"
  },
  "scripts": {
    "preinstall": "node ../../kibana/preinstall_check",
    "kbn": "node ../../kibana/scripts/kbn",
    "es": "node ../../kibana/scripts/es",
    "lint": "../../kibana/node_modules/.bin/eslint .",
    "start": "../../kibana/node_modules/.bin/plugin-helpers start --oss --no-watch",
    "test:server": "../../kibana/node_modules/.bin/plugin-helpers test:server",
    "test:browser": "../../kibana/node_modules/.bin/plugin-helpers test:browser",
    "build": "../../kibana/node_modules/.bin/plugin-helpers build"
  },
  "dependencies": {
    "expr-eval": "^1.2.2"
  }
}
